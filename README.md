Kotosiro Sharing Examples
==============================

 This directory contains example code snippets and notebooks.

Setup
==============================

 To set up the virtual environment, run the following commands in this directory:
 
 ```bash
  $ pyenv local 3.8.13
  $ pip install pipenv
  $ pipenv install -r requirements.txt
  $ pipenv shell
 ```
